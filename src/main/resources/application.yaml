spring:
  application:
    name: template-backend

  datasource:
    url: jdbc:mariadb://localhost:3306/chatM347BackendDb
    username: user
    password: 1234

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update

  mongodb:
    uri: mongodb://root:1234@localhost/WeatherMeassuresVorlage?authSource=admin
